[project]
name = "eclipseos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dannov", email = "danielseleznov8@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "py-cpuinfo>=9.0.0",
]

[project.scripts]
eclipseos = "eclipseos.main:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
